gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# NOTE: xplay-release.jks IS committed on purpose so every build (local or
# CI) produces the same signature and APK updates install over each other.
# Passwords live in key.properties, which stays out of git.
**/*.keystore
**/*.jks
